# Task 0008: CLI

Status: todo
Depends on: 0003
Parallel: yes — group A (owns `src/cli*` / bin entry)

## Goal

`yaml-form <input.yaml> [-o <output.html>]` works end-to-end: parse →
validate → generate → write file or stdout.

## Context

- Relevant docs: `docs/reference.md` (CLI)
- Interface: `-o/--output` (default stdout), `-h/--help`, `--version`.
  `upgrade` subcommand is task 0009 (print "not yet available" placeholder or
  leave unregistered here).

## Scope

- Argument parsing, exit codes (0 success; non-zero with all validation
  errors on stderr), stdout piping, `--help`/`--version` output.
- npm `bin` wiring in package.json.

## Out of scope

- Distribution builds and `upgrade` (task 0009).

## TDD plan

1. **Red** — tests (spawn the CLI): valid input → HTML on stdout / written to
   `-o` path; invalid YAML → non-zero exit, errors on stderr, no output file;
   missing args → usage; `--version` matches package.json.
2. **Green** — implement.
3. **Refactor** — keep CLI a thin shell over library functions (testable
   without spawning).

## Acceptance criteria

- [ ] `bunx . examples/sample.yaml -o /tmp/form.html` produces a working form
- [ ] Error and help output are clear; exit codes correct
- [ ] `bun test` and `bun run typecheck` pass

## Verification

- `bun test tests/cli`
